PORT=5000
# Local dev: USE_MEMORY_DB=true → không cần Atlas
USE_MEMORY_DB=true
# Vercel / production: thay MAT_KHAU (URL-encode nếu có @ # % / ...)
# Lấy URI: Atlas → Connect → Drivers → copy → thay <password>
MONGODB_URI=mongodb+srv://anhpt279_db_user:MAT_KHAU@cluster0.kacspxx.mongodb.net/hoi_choi_hoa?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=your-super-secret-jwt-key-change-in-production
